import { withRouter } from "next/router";
import Note from '../../components/layouts/note';

export const meta = {
  published: true,
  publishedAt: "2019-10-09",
  title: "First note",
};

export default withRouter(({ children, router }) => (
  <Note path={router.pathname} meta={meta}>
    {children}
  </Note>
));

# First Note

Hello, world.